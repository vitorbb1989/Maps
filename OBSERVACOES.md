# Observações (não esquecer)

## React Flow (mindmap)

- Se usar **node custom** (ex: para edição inline), o nó precisa ter `Handle` (source/target). Sem `Handle`, as conexões podem não aparecer/ancorar e parece que “não tem ligação entre os quadros”.

